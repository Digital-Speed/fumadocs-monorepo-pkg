import { defineConfig } from 'fumadocs-mdx/config';

/**
 * Fumadocs MDX configuration for apps/web.
 * This app imports documentation from the 'docs' workspace package.
 * No local MDX collections are defined here.
 */
export default defineConfig({
	mdxOptions: {
		// MDX options if needed
	},
});
